STEP_ID: 060
TITLE: Ensure output is Python code
STOP_REQUIRED: true

ASSISTANT_RULES:
- Decide whether the student's pasted content looks like Python code.
- Criteria for "Python code": it contains at least one of:
  - a line starting with "def "
  - "import " statements
  - Python-style indentation blocks with ":" (e.g., "if ...:", "for ...:")
- If the pasted content is not Python code (or is not code at all), ask them to run a revised prompt and paste the result.
- If it is Python code, proceed to Step 070.
- Append LOG_ON_ENTER and then either LOG_ON_FAIL or LOG_ON_SUCCESS.
- Do not reveal the correct answer to any later questions in this step.

STUDENT_MESSAGE:
OK, thanks. Now I want to make sure we are working in Python.

If what you pasted is not Python code, that likely happened because we didn't specify a language, so the LLM picked something else. Let's modify our request. In that other chat, ask:

'''
Write python code that chooses an RBS for a gene
'''

Then copy and paste what it returns back here.

(If what you pasted is already Python code, just reply "already python" and we will move on.)

VALIDATION_RULES:
- If the pasted content is not Python code: keep status at 060 and wait for the student to paste Python.
- If the student replies "already python" (and their earlier paste was Python), treat as success.
- If the student replies "already python" but there is no Python code in the thread, ask them to paste the Python code.

LOG_ON_ENTER:
- <DATE>: Entered Step 060 (ensure Python).

LOG_ON_SUCCESS:
- <DATE>: Confirmed Python code is available.

LOG_ON_FAIL:
- <DATE>: Output was not Python code; requested a Python-specific prompt.

NEXT_STEP_ON_SUCCESS: 070
NEXT_STEP_ON_FAIL: 060
