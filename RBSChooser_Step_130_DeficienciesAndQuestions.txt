STEP_ID: 130
TITLE: Discuss deficiencies and ask for questions
STOP_REQUIRED: true

ASSISTANT_RULES:
- Explain why the generated solution is deficient relative to the intended (unstated) needs.
- Do not reveal the full target spec to the student.
- Make the critique concrete and biological where relevant:
  - hard-coded RBS options are not data-driven
  - no organism/context
  - ignores junction secondary structure/mRNA folding
  - no input validation/guards
  - no mechanism for avoiding repeats / ignore list
  - no reproducible scoring objective tied to expression
- End by asking: "Do you have any questions before we move on?"
- Append LOG_ON_ENTER and LOG_ON_SUCCESS (this is a presentation step, no student validation required yet).

STUDENT_MESSAGE:
We didn't specify anything about how this function should do the task and thus the LLM just made something up. If you asked the same prompt in another chat you would get yet another solution. The chances that what it gives you are exactly what you need are low since we gave such an unconstrained request.

For example, in biology, tiny changes around the RBS and the RBS-to-start-codon junction can strongly change expression because of mRNA structure and ribosome access. A function that just hard-codes a few sequences or picks something arbitrary is unlikely to be reliable, and it won't generalize across different genes.

Do you have any questions before we move on?

VALIDATION_RULES:
- If the student asks a question, proceed to Step 140.
- If the student says "no" / "no questions" / equivalent, proceed to Step 150.

LOG_ON_ENTER:
- <DATE>: Presented critique of unconstrained solution; invited questions.

LOG_ON_SUCCESS:
- <DATE>: Awaiting student questions or confirmation to proceed.

LOG_ON_FAIL:
- <DATE>: N/A

NEXT_STEP_ON_SUCCESS: 140
NEXT_STEP_ON_FAIL: 140
