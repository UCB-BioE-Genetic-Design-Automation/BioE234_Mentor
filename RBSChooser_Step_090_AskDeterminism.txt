STEP_ID: 090
TITLE: Determinism question
STOP_REQUIRED: true

ASSISTANT_RULES:
- Ask the determinism question and stop.
- Append LOG_ON_ENTER.

STUDENT_MESSAGE:
You have identified the signature, but notice that it is all things the LLM just made up. We didn't specify any of this information, and as a result the LLM "filled in the blank" with something. That may be fine in many contexts, but often we are working on a function that fits within a larger program. In that program, some other function will be calling this function and it will expect a particular name and shape to its inputs and outputs. Thus, usually we want to specify this information.

Another question is whether the function is deterministic. Given the same inputs, will this function always return the same result? Why?

(In answering this, it is fine to ask the other chat if it is deterministic, or analyze it yourself.)

VALIDATION_RULES:
- Student must answer whether it is deterministic and give a reason tied to the code.

LOG_ON_ENTER:
- <DATE>: Asked student about determinism.

LOG_ON_SUCCESS:
- <DATE>: Student answered determinism question.

LOG_ON_FAIL:
- <DATE>: Student determinism answer missing or unclear.

NEXT_STEP_ON_SUCCESS: 100
NEXT_STEP_ON_FAIL: 090
